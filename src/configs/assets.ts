import { Asset } from '@zooizooi/asset-loader';

/**
 * Example Asset:
    {
        name: '',
        path: '',
        type: ''
    },
 */

const models: Asset[] = [
];

const textures: Asset[] = [
];

const assets = [
    ...models,
    ...textures,
];

export default assets;